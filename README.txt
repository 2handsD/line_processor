README

CS 344 Assignment 3
Dennis Ayres
Fall 2020

To compile and run the application, type the following commands
into the terminal:

     gcc line_processor.c -pthread -o line_processor


Then, to run the executable file, type:

	./line_processor